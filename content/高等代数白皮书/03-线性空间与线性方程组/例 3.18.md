---
chapter: 3
type: 例题
depends_on: []
---

# 例 3.18

## 依赖于
- 无显式依赖

## 被以下题目直接调用
- [[第 3 章填空题 7]]
- [[第 3 章填空题 8]]

> [!question] 例 3.18
> 设 $\alpha_1,\alpha_2,\cdots,\alpha_m$ 是一组线性无关的向量，
> 向量组 $\beta_1,\beta_2,\cdots,\beta_k$ 可用
> $\alpha_1,\alpha_2,\cdots,\alpha_m$ 线性表示如下：
>
> $$
>
> \left\{
> \begin{aligned}
> \beta_1&=a_{11}\alpha_1+a_{12}\alpha_2+\cdots+a_{1m}\alpha_m,\\
> \beta_2&=a_{21}\alpha_1+a_{22}\alpha_2+\cdots+a_{2m}\alpha_m,\\
> &\ \vdots\\
> \beta_k&=a_{k1}\alpha_1+a_{k2}\alpha_2+\cdots+a_{km}\alpha_m.
> \end{aligned}
> \right.
>
> $$
>
> 记表示矩阵 $A=(a_{ij})_{k\times m}$，求证：向量组
> $\beta_1,\beta_2,\cdots,\beta_k$ 的秩等于 $\operatorname{r}(A)$。

## 解答
**证明**  设 $\operatorname{r}(A)=r$，记 $A$ 的 $k$ 个行向量为
$\gamma_1,\gamma_2,\cdots,\gamma_k$。不失一般性，可假设 $A$ 的前 $r$ 个行向量
线性无关，其余行向量均可用前 $r$ 个行向量线性表示。若

$$

\gamma_i=c_1\gamma_1+c_2\gamma_2+\cdots+c_r\gamma_r,

$$

则经过简单计算可得

$$

\beta_i=c_1\beta_1+c_2\beta_2+\cdots+c_r\beta_r.

$$

另一方面，若

$$

c_1\beta_1+c_2\beta_2+\cdots+c_r\beta_r=0,

$$

则

$$

c_1(a_{11}\alpha_1+\cdots+a_{1m}\alpha_m)+\cdots+
c_r(a_{r1}\alpha_1+\cdots+a_{rm}\alpha_m)=0,

$$

即

$$

(c_1a_{11}+\cdots+c_ra_{r1})\alpha_1+\cdots+
(c_1a_{1m}+\cdots+c_ra_{rm})\alpha_m=0.

$$

因为 $\alpha_1,\cdots,\alpha_m$ 线性无关，故可得

$$

\left\{
\begin{aligned}
a_{11}c_1+a_{21}c_2+\cdots+a_{r1}c_r&=0,\\
a_{12}c_1+a_{22}c_2+\cdots+a_{r2}c_r&=0,\\
&\ \vdots\\
a_{1m}c_1+a_{2m}c_2+\cdots+a_{rm}c_r&=0.
\end{aligned}
\right.

$$

将上述方程组看成是未知数 $c_i$ 的齐次线性方程组，其系数矩阵的秩为 $r$，未知数个数
也是 $r$，因此只有唯一一组解，即零解。这表明
$\beta_1,\beta_2,\cdots,\beta_r$ 是向量组
$\beta_1,\beta_2,\cdots,\beta_k$ 的极大无关组，因此向量组
$\beta_1,\beta_2,\cdots,\beta_k$ 的秩等于 $r$。$\square$
