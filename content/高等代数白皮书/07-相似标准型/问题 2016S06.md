---
chapter: 7
type: 题目
depends_on:
  - 例 2.36
  - 例 2.38
tags:
  - 复旦习题集
---


# 问题 2016S06

## 依赖于
- [[例 2.36]]
- [[例 2.38]]

## 被以下题目直接调用
- 无

> [!question] 问题 2016S06
> (1) 设 $\boldsymbol{A}(\lambda)=(a_{ij}(\lambda))$ 是 n 阶 $\lambda-$ 矩阵, 则其行列式定义为
>
> $$
> | \boldsymbol {A} (\lambda) | = \sum_ {(i _ {1}, i _ {2}, \dots , i _ {n}) \in S _ {n}} (- 1) ^ {N (i _ {1}, i _ {2}, \dots , i _ {n})} a _ {i _ {1} 1} (\lambda) a _ {i _ {2} 2} (\lambda) \dots a _ {i _ {n} n} (\lambda).
> $$
>
> 利用上述定义证明: $n$ 阶 $\lambda$ -矩阵的行列式满足 9 条性质, 其中前 8 条参考高代教材 [1] § 1.3 和 § 1.4, 第 9 条性质参考高代教材的定理 1.4.1 和定理 1.4.2.
>
> (2)
> 证明: $\lambda$ -矩阵的行列式满足 Laplace 定理和 Cauchy-Binet 公式. 特别地, 设 $A(\lambda), B(\lambda)$ 为 $n$ 阶 $\lambda$ -矩阵, 则
>
> $$
> \left| \boldsymbol {A} (\lambda) \cdot \boldsymbol {B} (\lambda) \right| = \left| \boldsymbol {A} (\lambda) \right| \cdot \left| \boldsymbol {B} (\lambda) \right|,
> $$
>
> 即 $\lambda-$ 矩阵乘积的行列式等于其行列式的乘积.
>
> (3)
> 设 $n (n \geq 2)$ 阶 $\lambda$ -矩阵 $\boldsymbol{A}(\lambda)$ 的伴随矩阵为 $\boldsymbol{A}(\lambda)^*$ , 它的元素即为 $\boldsymbol{A}(\lambda)$ 中元素的代数余子式, 因此 $\boldsymbol{A}(\lambda)^*$ 也是一个 $n$ 阶 $\lambda$ -矩阵. 设 $\boldsymbol{A}(\lambda), \boldsymbol{B}(\lambda)$ 为 $n (n \geq 2)$ 阶 $\lambda$ -矩阵, 证明 $\lambda$ -矩阵的伴随矩阵满足如下性质:
>
> (3.1) $A(\lambda)A(\lambda)^{*} = A(\lambda)^{*}A(\lambda) = |A(\lambda)|I_n;$
>
> (3.2) $(\mathbf{A}(\lambda)\mathbf{B}(\lambda))^* = \mathbf{B}(\lambda)^*\mathbf{A}(\lambda)^*$;
>
> (3.3) $|\mathbf{A}(\lambda)^{*}| = |\mathbf{A}(\lambda)|^{n - 1};$
>
> (3.4) $(\mathbf{A}(\lambda)^{*})^{*} = |\mathbf{A}(\lambda)|^{n - 2}\mathbf{A}(\lambda)$.
>
> (4)
> 设 $A \in M_{n}(\mathbb{K})$ 的特征多项式 $f(\lambda) = |\lambda I_{n} - A|$，试对特征矩阵 $\lambda I_{n} - A$ 利用 (3.1) 证明 Cayley-Hamilton 定理，即 $f(A) = O$ .
> (5)
> 设 $A(\lambda)$ 为 n 阶 $\lambda-$ 矩阵, 证明下列结论等价:
>
> (5.1) $A(\lambda)$ 是可逆 $\lambda-$ 矩阵;
>
> (5.2) $A(\lambda)$ 的行列式是非零常数;
>
> (5.3) $A(\lambda)$ 的相抵标准型是 $I_{n}$ ;
>
> (5.4) $A(\lambda)$ 只通过 $\lambda-$ 矩阵的初等行变换或初等列变换就可变为 $I_{n}$ ;
>
> (5.5) $A(\lambda)$ 是有限个初等 $\lambda-$ 矩阵的乘积,
>
> 上述结论之一成立时, $A(\lambda)^{-1} = \frac{1}{|A(\lambda)|} A(\lambda)^*$ .

## 解答
(1) <https://www.cnblogs.com/torsor/p/3554028.html>, 参考教学博文《行列式的组合定义及其应用---反对称阵的 Pfaffian》.

(2)
参考高代教材中的证明, 由行列式的组合定义可推出 Laplace 定理, 由 Laplace 定理可推出 Cauchy-Binet 公式.
(3)
由行列式的性质可推出 (3.1). 参考[[例 2.36]], 由 Cauchy-Binet 公式可推出 (3.2). 可利用多项式的性质证明 (3.3). 设 $g(\lambda) = |\mathbf{A}(\lambda)^*| - |\mathbf{A}(\lambda)|^{n-1}$, 由[[例 2.38]] 可知, 对任意的 $k \in \mathbb{K}$ , $g(k) = |\mathbf{A}(k)^*| - |\mathbf{A}(k)|^{n-1} = 0$ , 于是 $g(\lambda)$ 在 $\mathbb{K}$ 中有无穷多个根, 从而 $g(\lambda)$ 为零多项式, 这就证明了结论. 同理, 可利用多项式的性质证明 (3.4), 以及给出 (2), (3.1) 和 (3.2) 的另一证明.
(4)
设特征多项式 $f(\lambda)=\lambda^{n}+a_{1}\lambda^{n-1}+\cdots+a_{n-1}\lambda+a_{n}, (\lambda\boldsymbol{I}_{n}-\boldsymbol{A})^{*}=M_{n-1}\lambda^{n-1}+\cdots+M_{1}\lambda+M_{0}$，则由 $(\lambda\boldsymbol{I}_{n}-\boldsymbol{A})(\lambda\boldsymbol{I}_{n}-\boldsymbol{A})^{*}=f(\lambda)\boldsymbol{I}_{n}$ 展开后比较矩阵多项式的系数，依

次可得: $M_{n-1}=I_{n}, M_{n-2}=AM_{n-1}+a_{1}I_{n}=A+a_{1}I_{n}, M_{n-3}=AM_{n-2}+a_{2}I_{n}=A^{2}+a_{1}A+a_{2}I_{n}, \cdots, M_{0}=AM_{1}+a_{n-1}I_{n}=A^{n-1}+a_{1}A^{n-2}+\cdots+a_{n-1}I_{n}$，最后可得

$$
\boldsymbol {O} = \boldsymbol {A} \boldsymbol {M} _ {0} + a _ {n} \boldsymbol {I} _ {n} = \boldsymbol {A} ^ {n} + a _ {1} \boldsymbol {A} ^ {n - 1} + \dots + a _ {n - 1} \boldsymbol {A} + a _ {n} \boldsymbol {I} _ {n} = f (\boldsymbol {A}).
$$

(5)
参考高代正课教学视频 § 7.2 中的证明.
