---
noteId: "高等代数白皮书/08-二次型/例-8.47"
shortId: 602
chapter: 8
type: 例题
depends_on: []
tags:
  - 高代白皮书
---

# 例 8.47

## 依赖于
- 无显式依赖

## 被以下题目直接调用
- [[例 8.51]]

> [!question] 例 8.47
> 设 $A$ 为 $n$ 阶正定实对称矩阵，$n$ 维实列向量 $\alpha,\beta$ 满足 $\alpha'\beta>0$，求证：
>
> $$
>
> H=A-\frac{A\beta\beta'A}{\beta'A\beta}+\frac{\alpha\alpha'}{\alpha'\beta}
>
> $$
>
> 是正定阵。

## 解答
**证明**
根据定义只要证明对任一实列向量 $x$，均有 $x'Hx\geq0$，且等号成立当且仅当 $x=0$ 即可。
一方面，由 $\alpha'\beta>0$ 可知

$$

\frac{x'(\alpha\alpha')x}{\alpha'\beta}
=\frac{(\alpha'x)^2}{\alpha'\beta}\geq0,

$$

等号成立当且仅当 $\alpha'x=0$。另一方面，由 $A$ 正定可知，存在非异实矩阵 $C$，使得
$A=C'C$。设

$$

C\beta=(b_1,b_2,\cdots,b_n)',   Cx=(x_1,x_2,\cdots,x_n)',

$$

则由 Cauchy-Schwarz 不等式可知

$$

\begin{aligned}
x'Ax-\frac{x'A\beta\beta'Ax}{\beta'A\beta}
&=(Cx)'(Cx)-\frac{(Cx)'(C\beta)(C\beta)'(Cx)}{(C\beta)'(C\beta)}\\
&=\left(\sum_{i=1}^n b_i^2\right)^{-1}
\left(\left(\sum_{i=1}^n b_i^2\right)\left(\sum_{i=1}^n x_i^2\right)
-\left(\sum_{i=1}^n b_ix_i\right)^2\right)\geq0.
\end{aligned}

$$

等号成立当且仅当 $b_i$ 与 $x_i$ 成比例，即存在实数 $k$，使得 $Cx=kC\beta$，即 $x=k\beta$。
由上述计算可得 $x'Hx\geq0$，且等号成立当且仅当 $\alpha'x=0$ 且 $x=k\beta$，再由
$\alpha'\beta>0$ 可得 $k=0$，从而 $x=0$，结论得证。$\square$
