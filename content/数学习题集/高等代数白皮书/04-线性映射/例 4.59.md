---
chapter: 4
type: 例题
depends_on: []
tags:
  - 高代白皮书
---

# 例 4.59

## 依赖于
- 无显式依赖

## 被以下题目直接调用
- [[第 4 章解答题 10]]

> [!question] 例 4.59
> 设 $\varphi_1,\cdots,\varphi_m$ 是 $n$ 维线性空间 $V$ 上的线性变换，且适合条件：
>
> $$
>
> \varphi_i^2=\varphi_i,  \varphi_i\varphi_j=0\ (i\neq j),
> \operatorname{Ker}\varphi_1\cap\cdots\cap\operatorname{Ker}\varphi_m=0.
>
> $$
>
> 求证：$V$ 是 $\operatorname{Im}\varphi_1,\cdots,\operatorname{Im}\varphi_m$ 的直和。

## 解答
**证明**  任取 $\alpha\in\operatorname{Im}\varphi_i\cap\left(\sum_{j\neq i}\operatorname{Im}\varphi_j\right)$，设 $\alpha=\varphi_i(\beta)$，其中 $\beta\in V$，则 $\varphi_i(\alpha)=\varphi_i^2(\beta)=\varphi_i(\beta)=\alpha$。又可设

$$

\alpha=\varphi_1(\alpha_1)+\cdots+\varphi_{i-1}(\alpha_{i-1})
+\varphi_{i+1}(\alpha_{i+1})+\cdots+\varphi_m(\alpha_m),

$$

于是

$$

\alpha=\varphi_i(\alpha)=\varphi_i\bigl(\varphi_1(\alpha_1)+\cdots+\varphi_{i-1}(\alpha_{i-1})
+\varphi_{i+1}(\alpha_{i+1})+\cdots+\varphi_m(\alpha_m)\bigr)=0.

$$

因此 $\operatorname{Im}\varphi_i\cap\left(\sum_{j\neq i}\operatorname{Im}\varphi_j\right)=0$。

对 $V$ 中任一向量 $\alpha$ 以及任意的 $i$，有

$$

\varphi_i\bigl(\alpha-(\varphi_1(\alpha)+\cdots+\varphi_m(\alpha))\bigr)
=\varphi_i(\alpha)-\varphi_i^2(\alpha)=0,

$$

因此

$$

\alpha-(\varphi_1(\alpha)+\cdots+\varphi_m(\alpha))
\in\operatorname{Ker}\varphi_1\cap\cdots\cap\operatorname{Ker}\varphi_m=0,

$$

从而 $\alpha-(\varphi_1(\alpha)+\cdots+\varphi_m(\alpha))=0$，即 $\alpha=\varphi_1(\alpha)+\cdots+\varphi_m(\alpha)$，于是 $V=\operatorname{Im}\varphi_1+\cdots+\operatorname{Im}\varphi_m$。这就证明了 $V$ 是 $\operatorname{Im}\varphi_1,\cdots,\operatorname{Im}\varphi_m$ 的直和。$\square$
