---
chapter: 7
type: 题目
depends_on:
  - 例 7.19
  - 例 7.56
tags:
  - 复旦习题集
---


# 22 级高代 II 期中 07

## 依赖于
- [[例 7.19]]
- [[例 7.56]]

## 被以下题目直接调用
- 无

> [!question] 22 级高代 II 期中 07
> 设 $A$ 是有理数域上的 $n$ 阶方阵，满足
>
> $$
> A^3=I_n,\qquad A\neq I_n.
> $$
>
> 证明：对任意给定的整数 $m$，存在有理数域上的 $n$ 阶方阵 $B$，使得 $B$ 相似于 $A$，且 $B$ 的所有元素之和等于 $m$。

## 解答
由[[例 7.19|例 7.19]] 可用有理标准型处理；第一类情形还会用到[[例 7.56|例 7.56]] 的证明思路。

由

$$
A^3=I_n
$$

可知 $A$ 在复数域上可对角化，特征值只能为

$$
1,\quad \omega,\quad \omega^2,
\qquad
\omega=-\frac12+\frac{\sqrt3}{2}i.
$$

由于 $A$ 是有理矩阵，非实特征值必须成共轭对出现；又 $A\neq I_n$，所以至少出现一对 $\omega,\omega^2$。故 $A$ 在复数域上相似于

$$
\operatorname{diag}(\omega,\omega^2,\ldots,\omega,\omega^2,1,\ldots,1),
$$

其中有 $k\geq 1$ 对 $\omega,\omega^2$，以及 $n-2k$ 个 $1$。

令

$$
C=
\begin{pmatrix}
0&-1\\
1&-1
\end{pmatrix}.
$$

这是多项式 $x^2+x+1$ 的友阵，并且在复数域上与

$$
\operatorname{diag}(\omega,\omega^2)
$$

相似。因此 $A$ 与

$$
F=\operatorname{diag}(C,\ldots,C,I_{n-2k})
$$

在复数域上相似。由于 $A,F$ 都是有理矩阵，它们事实上在 $\mathbb Q$ 上相似。也可以直接用[[例 7.19|例 7.19]] 的有理标准型得到这一点。

接下来分三种情形。

### 情形一：$n-2k\geq 1$

此时 $F$ 中既有 $C$ 块，也有 $1$ 特征值块。$C$ 部分与 $I_{n-2k}$ 部分没有公共特征值。沿用[[例 7.56|例 7.56]] 的证明方法，可把 $F$ 相似变换为如下形式：

$$
B=
\begin{pmatrix}
C&&&&&x\\
&\ddots&&&&\\
&&C&&&\\
&&&1&&\\
&&&&\ddots&\\
&&&&&1
\end{pmatrix}.
$$

取

$$
x=m+3k-n.
$$

这样得到的 $B$ 与 $F$ 相似，从而与 $A$ 相似；并且 $B$ 的所有元素之和正好为 $m$。

### 情形二：$n-2k=0$ 且 $k\geq 2$

此时 $F$ 全由 $C$ 块组成。对 $F$ 作相似初等变换：第一列乘以 $x$ 加到第四列，再将第四行乘以 $-x$ 加到第一行。只看前四行四列，变换效果为

$$
\begin{pmatrix}
0&-1&&\\
1&-1&&\\
&&0&-1\\
&&1&-1
\end{pmatrix}
\longrightarrow
\begin{pmatrix}
0&-1&-x&x\\
1&-1&0&x\\
&&0&-1\\
&&1&-1
\end{pmatrix}.
$$

令

$$
x=m+k.
$$

则所得矩阵 $B$ 仍与 $F$ 相似，因而与 $A$ 相似，并且元素总和为 $m$。

### 情形三：$n=2$ 且 $k=1$

此时 $A$ 相似于友阵 $C$。设

$$
B=
\begin{pmatrix}
a&b\\
c&d
\end{pmatrix}.
$$

要使 $B$ 与 $C$ 相似，只需让 $B$ 的特征多项式等于

$$
x^2+x+1.
$$

再加上元素之和为 $m$，得到方程组

$$
a+d=-1,\qquad ad-bc=1,\qquad a+b+c+d=m.
$$

化简后可转化为

$$
b(b-m-1)-a(a+1)=1,
$$

也就是

$$
\left(b-\frac{m+1}{2}\right)^2
-
\left(a+\frac12\right)^2
=
\frac{m^2+2m+4}{4}.
$$

取一组有理解：

$$
a=\frac{(m+1)^2}{4},\quad
b=\frac{m^2+4m+7}{4},\quad
c=-\frac{m^2+3}{4},\quad
d=-\frac{m^2+2m+5}{4}.
$$

这时 $B$ 是有理矩阵，与 $C$ 相似，因而与 $A$ 相似；同时 $B$ 的所有元素之和为 $m$。

三种情况覆盖全部可能，命题得证。

参考：[谢启鸿高等代数官方博客](https://www.cnblogs.com/torsor/p/17418461.html)。
